#include <iostream>
using namespace std;

int main() {
    int A, B, X;

    // Lê os valores de A e B
    cin >> A >> B;

    // Soma os valores de A e B
    X = A + B;

    // Imprime o resultado no formato especificado
    cout << "X = " << X << endl;

    return 0;
}
